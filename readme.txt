1)	first install node modules by 
	npm i or yarn

2)	Then link depandances 
	npx react-native link

3)	build the project
	npx react-native run-andrid

	if can't build the project then delete yarn lock 
	delete node module and run npx audit fix --force
